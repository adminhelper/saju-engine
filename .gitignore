# ── OS ──────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── 에디터/IDE ──────────────────────────────────────
.vscode/
.idea/
*.swp
*~

# ── Node (엔진은 zero-dep 이지만 만일 대비) ──────────
node_modules/
npm-debug.log*
*.log

# ── OMC 세션/워크플로우 상태 (플러그인 본체 아님) ────
.omc/

# ── 생성된 사주 보고서 (산출물 — 저장소에 커밋하지 않음) ──
#    examples/ 안의 샘플은 추적함(공개 예시). 루트의 생성물만 제외.
/saju-*.html
/out/
*.local.html
